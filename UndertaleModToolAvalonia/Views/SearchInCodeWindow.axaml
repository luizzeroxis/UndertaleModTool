<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:v="clr-namespace:UndertaleModToolAvalonia.Views"
        xmlns:assets="clr-namespace:UndertaleModToolAvalonia.Assets"
        mc:Ignorable="d"
        x:Class="UndertaleModToolAvalonia.Views.SearchInCodeWindow"
        x:DataType="v:SearchInCodeViewModel"
        Title="Search in code"
        Width="800"
        Height="400"
        Classes="SearchInCodeWindow">
    <Grid RowDefinitions="Auto,1*,Auto">
        <StackPanel Grid.Row="0">
            <TextBox Name="SearchTextTextBox" Text="{Binding SearchText}" AcceptsReturn="True"/>
            <StackPanel Orientation="Horizontal">
                <CheckBox IsChecked="{Binding IsCaseSensitive}" Content="{x:Static assets:Resources.CaseSensitiveText}" />
                <CheckBox IsChecked="{Binding IsRegexSearch}" Content="{x:Static assets:Resources.RegexSearchText}" />
                <CheckBox IsChecked="{Binding IsInAssembly}" Content="{x:Static assets:Resources.InAssemblyText}" />
            </StackPanel>
            <Button Command="{Binding Search}" IsDefault="True" HorizontalAlignment="Right">Search</Button>
        </StackPanel>
        <DataGrid Grid.Row="1" Name="ResultsDataGrid" ItemsSource="{Binding Results}" IsReadOnly="True" CanUserResizeColumns="True"
                  DoubleTapped="DataGrid_DoubleTapped">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static assets:Resources.LocationText}" Binding="{Binding Location}" Width="Auto" />
                <DataGridTextColumn Header="{x:Static assets:Resources.TextText}" Binding="{Binding Text}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="2" Text="{Binding StatusBarText}" Classes="StatusBar"/>
    </Grid>
</Window>
